import 'package:flutter/material.dart';

class InspectionTab extends StatelessWidget {
  const InspectionTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Inspection — coming soon'));
  }
}
