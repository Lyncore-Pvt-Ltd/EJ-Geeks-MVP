name: Dev Build & Release

on:
  push:
    branches: [dev]
  workflow_dispatch: {}

env:
  FLUTTER_VERSION: "3.44.2"
  FLUTTER_CHANNEL: "stable"

jobs:
  prep:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    outputs:
      version: ${{ steps.version.outputs.version }}
      build: ${{ steps.version.outputs.build }}
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Bump build number in pubspec.yaml
        id: version
        run: |
          CURRENT=$(grep '^version:' pubspec.yaml | sed 's/version: *//')
          VERSION=$(echo "$CURRENT" | cut -d '+' -f1)
          BUILD=$(echo "$CURRENT" | cut -d '+' -f2)
          NEW_BUILD=$((BUILD + 1))

          sed -i "s/^version: .*/version: ${VERSION}+${NEW_BUILD}/" pubspec.yaml

          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "build=$NEW_BUILD" >> "$GITHUB_OUTPUT"

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add pubspec.yaml
          git commit -m "chore: bump build number to ${VERSION}+${NEW_BUILD} [skip ci]"
          git push origin HEAD:${{ github.ref_name }}

  build-android:
    needs: prep
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: ${{ env.FLUTTER_CHANNEL }}

      - name: Decode Android keystore
        run: echo "${{ secrets.ANDROID_KEYSTORE_BASE64 }}" | base64 -d > android/app/release-key.jks

      - name: Debug keystore secret length
        run: echo -n "${{ secrets.ANDROID_KEYSTORE_BASE64 }}" | wc -c

      - name: Write key.properties
        run: |
          cat > android/key.properties <<EOF
          storePassword=${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          keyPassword=${{ secrets.ANDROID_KEY_PASSWORD }}
          keyAlias=${{ secrets.ANDROID_KEY_ALIAS }}
          storeFile=release-key.jks
          EOF

      - run: flutter pub get

      - run: flutter build apk --release --build-name=${{ needs.prep.outputs.version }} --build-number=${{ needs.prep.outputs.build }}

      - name: Rename APK
        run: |
          cp build/app/outputs/flutter-apk/app-release.apk \
            "build/app/outputs/flutter-apk/ej-geek-beta-${{ needs.prep.outputs.version }}+${{ needs.prep.outputs.build }}.apk"

      - uses: actions/upload-artifact@v4
        with:
          name: android-apk
          path: build/app/outputs/flutter-apk/ej-geek-beta-${{ needs.prep.outputs.version }}+${{ needs.prep.outputs.build }}.apk
          retention-days: 7

  build-ios:
    needs: prep
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: ${{ env.FLUTTER_CHANNEL }}

      - run: flutter pub get

      # No Apple Developer signing assets are configured yet, so this only
      # verifies the iOS build compiles. It produces an unsigned Runner.app,
      # not an installable .ipa.
      #
      # To upgrade to a signed, installable IPA later:
      #   1. Add secrets: IOS_DIST_CERT_P12_BASE64, IOS_DIST_CERT_PASSWORD,
      #      IOS_PROVISIONING_PROFILE_BASE64
      #   2. Import the cert into a temporary keychain and install the
      #      provisioning profile under ~/Library/MobileDevice/Provisioning Profiles
      #   3. Add ios/ExportOptions.plist
      #   4. Replace the build step below with `flutter build ipa --export-options-plist=ios/ExportOptions.plist`
      - run: flutter build ios --release --no-codesign --build-name=${{ needs.prep.outputs.version }} --build-number=${{ needs.prep.outputs.build }}

      - name: Zip unsigned app
        run: |
          cd build/ios/iphoneos
          zip -r "ej-geek-beta-${{ needs.prep.outputs.version }}+${{ needs.prep.outputs.build }}-ios.zip" Runner.app

      - uses: actions/upload-artifact@v4
        with:
          name: ios-unsigned-app
          path: build/ios/iphoneos/ej-geek-beta-${{ needs.prep.outputs.version }}+${{ needs.prep.outputs.build }}-ios.zip
          retention-days: 7

  release:
    needs: [prep, build-android, build-ios]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Compute release tag
        id: tag
        run: echo "tag=dev-build-${{ needs.prep.outputs.version }}+${{ needs.prep.outputs.build }}" >> "$GITHUB_OUTPUT"

      - uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.tag.outputs.tag }}
          name: Dev build ${{ needs.prep.outputs.version }}+${{ needs.prep.outputs.build }} (${{ github.sha }})
          target_commitish: ${{ github.sha }}
          files: |
            artifacts/android-apk/ej-geek-beta-${{ needs.prep.outputs.version }}+${{ needs.prep.outputs.build }}.apk
            artifacts/ios-unsigned-app/ej-geek-beta-${{ needs.prep.outputs.version }}+${{ needs.prep.outputs.build }}-ios.zip
